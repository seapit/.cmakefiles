set(ARG1 "")
#set(ARG1 ".c")
set(ARG2 "")
#set(ARG2 "GENERATED)
include_directories(src)
enable_testing()
add_executable(${TARGETNAME}Test ${SRC_FILES})

add_Test(NAME ${TARGET_NAME}Test COMMAND ${TARGET_NAME}Test)

TEST( TARGET_NAME ${ARG1} ${ARG2})
